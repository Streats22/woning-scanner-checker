# ═══════════════════════════════════════════════════════════════════════════════
# Ploi: officiële NGINX-siteconfig voor dehuurradar.nl (repo = bron van waarheid)
# Nuxt (/) + Laravel (/api, /report, …). Plak dit in Ploi → NGINX → hoofdveld
# (≈ /etc/nginx/sites-available/dehuurradar.nl).
# ═══════════════════════════════════════════════════════════════════════════════
#
# Gebruik GEEN `include .../server/*` hier als je hieronder al alle locations hebt:
# die wildcard laadt o.a. server/dehuurradar.nl met nog een `location /` → duplicate.
# Acme-challenge staat hieronder inline.
#
# Geen error_page 404 /index.php (breekt Nuxt). Pas php-fpm-socket aan: ls /run/php/
# sudo nginx -t && sudo systemctl reload nginx
#
# Let's Encrypt / Ploi: bij 404 op /.well-known/acme-challenge/…
# - Ploi → Site → Web directory = public (zelfde pad als `root` hieronder).
# - SSL-certificaat aanvragen: webroot MOET zijn …/dehuurradar.nl/public (niet de site-root
#   boven `public`), anders schrijft Certbot op een plek die nginx niet uitleest.
# - Zorg dat `server_name` zowel apex als www bevat (zie hieronder).
# - Test: mkdir -p public/.well-known/acme-challenge && echo ok > public/.well-known/acme-challenge/t
#   curl -sS http://dehuurradar.nl/.well-known/acme-challenge/t  → moet "ok" tonen (geen 404/proxy).
# ═══════════════════════════════════════════════════════════════════════════════

# Ploi Webserver Configuration, do not remove!
include /etc/nginx/ploi/dehuurradar.nl/before/*;

server {
    listen 80;
    listen [::]:80;

    root /home/ploi/dehuurradar.nl/public;
    server_name dehuurradar.nl www.dehuurradar.nl dreamy-raccoon-nncbxrzz7r.ploi.ing;

    include /etc/nginx/ssl/dehuurradar.nl;

    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_ecdh_curve X25519:prime256v1:secp384r1;
    ssl_ciphers ECDHE-ECDSA-AES128-GCM-SHA256:ECDHE-RSA-AES128-GCM-SHA256:ECDHE-ECDSA-AES256-GCM-SHA384:ECDHE-RSA-AES256-GCM-SHA384:ECDHE-ECDSA-CHACHA20-POLY1305:ECDHE-RSA-CHACHA20-POLY1305:DHE-RSA-AES128-GCM-SHA256:DHE-RSA-AES256-GCM-SHA384:DHE-RSA-CHACHA20-POLY1305;
    ssl_prefer_server_ciphers off;
    ssl_dhparam /etc/nginx/dhparams.pem;

    index index.php index.html;

    add_header X-Frame-Options "SAMEORIGIN";
    add_header X-XSS-Protection "1; mode=block";
    add_header X-Content-Type-Options "nosniff";

    charset utf-8;

    # Let's Encrypt — expliciet bestand van schijf; anders valt de request door naar Nuxt → 404.
    # ^~ voorkomt dat een andere regex-location dit onderschept.
    location ^~ /.well-known/acme-challenge/ {
        allow all;
        auth_basic off;
        default_type "text/plain";
        root /home/ploi/dehuurradar.nl/public;
        try_files $uri =404;
    }

    # --- Nuxt + Laravel ---
    location ^~ /api {
        try_files $uri $uri/ /index.php?$query_string;
    }

    location ^~ /report {
        try_files $uri $uri/ /index.php?$query_string;
    }

    location = /up {
        try_files $uri $uri/ /index.php?$query_string;
    }

    location ^~ /build {
        try_files $uri $uri/ /index.php?$query_string;
    }

    location ^~ /storage {
        try_files $uri $uri/ /index.php?$query_string;
    }

    # Cloudflare → origin is vaak HTTP: $scheme = http terwijl de bezoeker HTTPS gebruikt.
    # Nitro gebruikt X-Forwarded-Proto voor absolute URLs (/_nuxt/*.js); zonder fix worden die http://.
    location / {
        set $forwarded_proto $scheme;
        if ($http_x_forwarded_proto != "") {
            set $forwarded_proto $http_x_forwarded_proto;
        }
        proxy_pass http://127.0.0.1:3000;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $forwarded_proto;
        proxy_set_header X-Forwarded-Host $host;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
    }

    access_log off;
    error_log  /var/log/nginx/dehuurradar.nl-error.log error;

    location = /favicon.ico { access_log off; log_not_found off; }
    location = /robots.txt  { access_log off; log_not_found off; }

    location ~ \.php$ {
        try_files $uri /index.php =404;
        fastcgi_split_path_info ^(.+\.php)(/.+)$;
        fastcgi_pass unix:/run/php/php8.5-fpm.sock;
        fastcgi_buffers 32 32k;
        fastcgi_index index.php;
        fastcgi_param SCRIPT_FILENAME $realpath_root$fastcgi_script_name;
        fastcgi_param DOCUMENT_ROOT $realpath_root;
        include fastcgi_params;
    }

    location ~ /\.(?!well-known).* {
        deny all;
    }
}

# Ploi Webserver Configuration, do not remove!
include /etc/nginx/ploi/dehuurradar.nl/after/*;
